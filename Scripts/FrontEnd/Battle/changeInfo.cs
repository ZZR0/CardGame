using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class changeInfo : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //test
        /*
        Global.GetInstance().Enemyenergy = "10/10";
        Global.GetInstance().Userpressure = "0/30";
        Global.GetInstance().Enemypressure = "0/30";*/
        //Debug.Log(Global.GetInstance().Username);

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
